<!-- geo-stack/osm/README.md -->
# OpenStreetMap integration

This directory contains configuration and utilities for OSM tile layers and OSM-specific geocoding.
In production, we use Leaflet with OSM tiles. Custom tile servers can be configured here.